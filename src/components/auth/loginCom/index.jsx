'use client'

import LoginEl from "@/elements/loginEl";
import { LayoutEl } from "@/shared/layoutEl";
const Login= () => {
    return(
        <LayoutEl>
            <LoginEl />
        </LayoutEl>
    );
}

export default Login;